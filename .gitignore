# --- Terraform local dirs & state ---
.terraform/
*.tfstate
*.tfstate.*
.terraform.tfstate.lock.info

# Crash logs
crash.log
crash.*.log

# --- Sensitive variables (never commit) ---
*.tfvars
*.tfvars.json
*.auto.tfvars
*.env

# SSH/keys/certs (belt & suspenders)
*.pem
*.key
*.pub
*.pfx
*.crt
*.cer
*.json

# Plans & backups
*.backup
*.log
*tfplan*

# Terraform CLI config
.terraformrc
terraform.rc

# OS/editor noise
.DS_Store
Thumbs.db
